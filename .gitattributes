# Normalize to LF in the repository. The hook scripts and skill files are read on
# Linux, macOS, and Windows, so a consistent checkout matters.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.zip binary
